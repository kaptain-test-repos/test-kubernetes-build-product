name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    uses: kube-kaptain/buildon-github-actions/.github/workflows/kubernetes-product-aggregate.yaml@p3
    permissions:
      contents: write
      packages: write
      checks: write
